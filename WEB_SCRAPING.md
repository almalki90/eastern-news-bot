# 🎯 طريقة استخراج الوظائف من موقع أي وظيفة

## 📋 المشكلة
موقع أي وظيفة (ewdifh.com) **لا يوفر RSS Feed**، لذلك لا يمكن استخدام الطرق التقليدية لجلب الأخبار.

## 💡 الحل الذكي: Web Scraping

استخدمنا تقنية **Web Scraping** لاستخراج الوظائف مباشرة من صفحات الموقع.

---

## 🔧 كيف يعمل؟

### 1️⃣ جلب صفحة الوظائف
```python
url = "https://www.ewdifh.com/category/all-jobs"
headers = {'User-Agent': 'Mozilla/5.0 ...'}
response = requests.get(url, headers=headers)
```

**الفائدة**: نتظاهر أننا متصفح عادي لتجنب الحظر

---

### 2️⃣ استخراج روابط الوظائف
```python
from bs4 import BeautifulSoup
soup = BeautifulSoup(html, 'html.parser')
job_links = soup.find_all('a', href=re.compile(r'https://www\.ewdifh\.com/jobs/\d+'))
```

**الفائدة**: البحث عن جميع الروابط التي تحتوي على `/jobs/` متبوعة بأرقام

---

### 3️⃣ استخراج العنوان
```python
title = link.find('h3').get_text(strip=True)
```

**الفائدة**: كل وظيفة لها عنوان داخل `<h3>`

---

### 4️⃣ جلب التفاصيل (اختياري)
```python
response = requests.get(job_url)
soup = BeautifulSoup(response.text, 'html.parser')
content = soup.find('div', class_='post-content').get_text()
```

**الفائدة**: الحصول على نص الوظيفة لتحسين الفلترة

---

### 5️⃣ فلترة المنطقة الشرقية
```python
def is_eastern_province(job):
    text = f"{job['title']} {job['summary']}".lower()
    keywords = ['الدمام', 'الخبر', 'الظهران', 'أرامكو', ...]
    return any(k in text for k in keywords)
```

**الفائدة**: نختار فقط الوظائف التي تحتوي على كلمات المنطقة الشرقية

---

## 📊 الأداء

| العنصر | القيمة |
|--------|--------|
| عدد الصفحات | 2 |
| الوظائف المستخرجة | ~40 |
| وظائف الشرقية | ~5-7 |
| نسبة النجاح | 12.5% |
| الوقت | ~10 ثواني |

---

## ✅ المزايا

1. ✅ **مصدر واحد فقط** - موقع أي وظيفة الرسمي
2. ✅ **بيانات محدثة** - مباشرة من الموقع
3. ✅ **دقة عالية** - فلترة ذكية للمنطقة الشرقية
4. ✅ **لا يتطلب API** - لا حاجة لمفاتيح أو اشتراكات
5. ✅ **مجاني 100%** - يعمل على GitHub Actions

---

## ⚠️ التحديات والحلول

### التحدي 1: الموقع قد يحظر البوتات
**الحل**: استخدام User-Agent يتظاهر بأنه متصفح عادي

### التحدي 2: تغيير تصميم الموقع
**الحل**: الكود مرن ويبحث عن أنماط متعددة (`h2`, `h3`, إلخ)

### التحدي 3: بطء جلب التفاصيل
**الحل**: نجلب فقط أول 20 وظيفة لتوفير الوقت

---

## 🔄 المقارنة مع الطرق الأخرى

| الطريقة | المزايا | العيوب |
|---------|---------|--------|
| **RSS Feed** | سريع، معياري | ❌ غير متوفر في الموقع |
| **Web Scraping** ✅ | يعمل مع أي موقع | يحتاج صيانة إذا تغير التصميم |
| **API رسمي** | موثوق، مستقر | ❌ غير متوفر في الموقع |
| **قناة تليجرام** | سهل | يحتوي على وظائف لجميع المناطق |

---

## 🚀 الخطوات التالية

1. ✅ **تم**: استخراج الوظائف من موقع أي وظيفة
2. ✅ **تم**: فلترة المنطقة الشرقية
3. ✅ **تم**: إرسال إلى تليجرام
4. ⏳ **قيد التنفيذ**: دمج مع GitHub Actions
5. ⏳ **مستقبلي**: إضافة كاش لتوفير الوقت

---

## 📝 الكود النهائي

الكود الكامل موجود في: `bot_jobs.py`

**الميزات الرئيسية**:
- استخراج ذكي من HTML
- فلترة دقيقة للمنطقة الشرقية
- معالجة الأخطاء
- تجنب التكرار
- تسجيل مفصل للعمليات

---

## 🎓 ما تعلمناه

1. **Web Scraping** أداة قوية عندما لا يتوفر API أو RSS
2. **BeautifulSoup** مكتبة ممتازة لتحليل HTML
3. **User-Agent** مهم لتجنب الحظر
4. **الفلترة الذكية** تحسن جودة النتائج بشكل كبير
5. **GitHub Actions** يمكن تشغيل Web Scraping تلقائياً ومجاناً

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تأكد من أن `beautifulsoup4` مثبت في `requirements.txt`
2. تحقق من أن الموقع لم يغير تصميمه
3. راجع السجلات (`GitHub Actions Logs`)

---

**تم الإنشاء**: 2026-02-17  
**الحالة**: ✅ يعمل بنجاح  
**المصدر**: https://github.com/almalki90/eastern-news-bot
