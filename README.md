# بوت تليجرام - أخبار المنطقة الشرقية 🤖📰

بوت تليجرام تلقائي يجلب أخبار المنطقة الشرقية من مصادر RSS سعودية موثوقة ويرسلها للمجموعات والقنوات التي هو مشرف فيها.

## ✨ المميزات

- ✅ **تلقائي 100%** - يعمل بدون أي تدخل منك
- ✅ **مجاني تماماً** - يعمل على GitHub Actions
- ✅ **جدولة ذكية** - يرسل الأخبار كل 6 ساعات
- ✅ **لا تكرار** - يتذكر الأخبار المرسلة سابقاً
- ✅ **مصادر متعددة** - يجلب من عدة مصادر إخبارية

## 📡 مصادر الأخبار

1. **وكالة الأنباء السعودية (واس)** - المصدر الرسمي
2. **عرب نيوز** - صحيفة عربية دولية
3. مصادر إضافية يمكن إضافتها

## 🚀 طريقة التثبيت

### 1️⃣ إنشاء البوت

1. افتح [@BotFather](https://t.me/BotFather) في تليجرام
2. أرسل `/newbot`
3. اختر اسم للبوت (مثلاً: Eastern Province News)
4. احفظ الـ **Bot Token**

### 2️⃣ إضافة البوت للمجموعات

1. أضف البوت لمجموعاتك/قنواتك
2. اجعله **مشرف** مع صلاحية إرسال الرسائل
3. أرسل رسالة في المجموعة حتى يسجلها البوت

### 3️⃣ رفع الكود على GitHub

```bash
# إنشاء repository جديد على GitHub
git init
git add .
git commit -m "Initial commit: Eastern Province News Bot"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/eastern-province-news-bot.git
git push -u origin main
```

### 4️⃣ إضافة Bot Token في GitHub

1. اذهب لـ Repository Settings
2. اختر **Secrets and variables** → **Actions**
3. اضغط **New repository secret**
4. الاسم: `BOT_TOKEN`
5. القيمة: التوكن من BotFather
6. احفظ

### 5️⃣ تفعيل GitHub Actions

1. اذهب لتبويب **Actions**
2. فعّل Workflows
3. يمكنك تشغيل البوت يدوياً من "Run workflow"

## ⚙️ الإعدادات

### تغيير التوقيت

عدّل ملف `.github/workflows/news-bot.yml`:

```yaml
schedule:
  - cron: '0 */6 * * *'  # كل 6 ساعات
  # أمثلة أخرى:
  # - cron: '0 */1 * * *'  # كل ساعة
  # - cron: '0 8,14,20 * * *'  # الساعة 8 صباحاً، 2 ظهراً، 8 مساءً
  # - cron: '0 9 * * *'  # يومياً الساعة 9 صباحاً
```

### إضافة Chat IDs يدوياً

إذا لم يعثر البوت على المجموعات تلقائياً، يمكنك إضافتها يدوياً في `bot.py`:

```python
# استبدل هذا السطر:
# chat_ids = get_bot_chats()

# بهذا (أضف IDs مجموعاتك):
chat_ids = [-1001234567890, -1009876543210]
```

**كيف تحصل على Chat ID؟**
1. أضف [@RawDataBot](https://t.me/RawDataBot) لمجموعتك
2. سيرسل لك معلومات المجموعة بما فيها `chat_id`

### إضافة مصادر RSS جديدة

عدّل القائمة في `bot.py`:

```python
RSS_FEEDS = [
    {
        'name': 'اسم المصدر',
        'url': 'https://example.com/rss.xml',
        'enabled': True
    }
]
```

## 📊 كيف يعمل؟

```
┌─────────────────────────────────────┐
│   GitHub Actions (كل 6 ساعات)      │
│                                     │
│  1. يجلب RSS feeds من المصادر      │
│  2. يقرأ sent_news.json             │
│  3. يرسل الأخبار الجديدة فقط       │
│  4. يحدث sent_news.json             │
│  5. يحفظ التغييرات على GitHub      │
└─────────────────────────────────────┘
```

## 🔍 مراقبة البوت

1. اذهب لتبويب **Actions** في Repository
2. شاهد آخر تشغيل للبوت
3. اضغط على Run لرؤية التفاصيل والأخطاء

## ❓ الأسئلة الشائعة

**س: لماذا لا يرسل البوت؟**
- تأكد من Bot Token صحيح في Secrets
- تأكد من البوت مشرف في المجموعات
- شاهد logs في GitHub Actions

**س: كيف أوقف البوت مؤقتاً؟**
- اذهب لـ Actions → اختر Workflow → Disable

**س: هل يمكن تشغيله على سيرفر خاص؟**
- نعم! شغّل `bot.py` بـ cron job على أي سيرفر

**س: كم مرة يعمل مجاناً؟**
- GitHub Actions مجاني للـ Public Repositories بدون حدود

## 📝 المتطلبات

- حساب GitHub (مجاني)
- بوت تليجرام من BotFather
- المجموعات/القنوات التي تريد إرسال الأخبار لها

## 🛠️ تطوير محلي

```bash
# تثبيت المكتبات
pip install -r requirements.txt

# تشغيل البوت
export BOT_TOKEN="توكن_البوت_هنا"
python bot.py
```

## 📜 الترخيص

MIT License - استخدم الكود بحرية!

## 🤝 المساهمة

مساهماتك مرحب بها! افتح Issue أو Pull Request.

---

**صُنع بـ ❤️ للمنطقة الشرقية** 🇸🇦
