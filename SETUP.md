# 🎉 البوت جاهز! - خطوات التفعيل

## ✅ ما تم إنجازه:

1. ✅ كتابة سكريبت Python يجلب أخبار من RSS feeds
2. ✅ إنشاء GitHub Actions workflow للتشغيل التلقائي
3. ✅ اختبار البوت - يعمل بنجاح! ✨
4. ✅ توثيق شامل في README.md

---

## 📋 الخطوات المتبقية (يجب عليك القيام بها):

### 1️⃣ رفع الكود على GitHub

```bash
# على جهازك المحلي أو في terminal:

# 1. إنشاء repository جديد على https://github.com/new
#    اسم مقترح: eastern-province-news-bot

# 2. رفع الكود
cd /home/user/webapp
git remote add origin https://github.com/YOUR_USERNAME/eastern-province-news-bot.git
git branch -M main
git push -u origin main
```

**💡 ملاحظة:** ستحتاج لإعداد GitHub authentication أولاً من خلال تبويب #github في واجهة المستخدم.

---

### 2️⃣ إضافة Bot Token في GitHub Secrets

1. اذهب لـ Repository على GitHub
2. **Settings** → **Secrets and variables** → **Actions**
3. اضغط **New repository secret**
4. **Name:** `BOT_TOKEN`
5. **Value:** `8281406621:AAGpJOnC1Ua1I4t49h8kWea-7pND8zTSBhg`
6. احفظ ✅

---

### 3️⃣ إضافة البوت للمجموعات

1. افتح تليجرام
2. اذهب لمجموعاتك/قنواتك
3. أضف البوت: `@YOUR_BOT_USERNAME`
4. اجعله **مشرف** (Admin) مع صلاحية إرسال رسائل
5. أرسل أي رسالة في المجموعة حتى يسجلها البوت

---

### 4️⃣ تفعيل GitHub Actions

1. اذهب لتبويب **Actions** في Repository
2. فعّل Workflows إذا طُلب منك
3. يمكنك تشغيل البوت يدوياً: **Run workflow**

---

### 5️⃣ (اختياري) إضافة Chat IDs يدوياً

إذا لم يعثر البوت على المجموعات تلقائياً:

1. أضف [@RawDataBot](https://t.me/RawDataBot) لمجموعتك
2. سينسخ لك `chat_id` (مثل: `-1001234567890`)
3. عدّل `bot.py` في السطر 84:

```python
# استبدل:
chat_ids = get_bot_chats()

# بـ:
chat_ids = [-1001234567890, -1009876543210]  # أضف IDs مجموعاتك
```

---

## 🔍 مراقبة البوت

- **GitHub Actions**: https://github.com/YOUR_USERNAME/eastern-province-news-bot/actions
- شاهد آخر تشغيل ولوقات البوت من هناك

---

## ⏰ جدولة التشغيل

البوت يعمل **كل 6 ساعات** تلقائياً.

لتغيير التوقيت، عدّل `.github/workflows/news-bot.yml`:

```yaml
schedule:
  - cron: '0 */6 * * *'  # كل 6 ساعات
```

**أمثلة أخرى:**
```yaml
- cron: '0 */1 * * *'    # كل ساعة
- cron: '0 8,14,20 * * *'  # 8 صباحاً، 2 ظهراً، 8 مساءً
- cron: '0 9 * * *'      # يومياً الساعة 9 صباحاً
```

---

## 📡 مصادر الأخبار الحالية

1. ✅ عرب نيوز - السعودية
2. ✅ عرب نيوز - آخر الأخبار
3. ✅ الشرق الأوسط
4. ✅ BBC Arabic
5. ✅ الجزيرة

يمكنك إضافة/حذف مصادر من `bot.py` (السطر 19-44)

---

## ❓ مشاكل شائعة

**❌ البوت لا يرسل:**
- تأكد من Bot Token في GitHub Secrets
- تأكد من البوت مشرف في المجموعات
- شاهد logs في GitHub Actions

**❌ لا توجد مجموعات:**
- أضف Chat IDs يدوياً (خطوة 5)
- تأكد من وجود رسائل سابقة في المجموعات

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. اقرأ README.md بالتفصيل
2. شاهد GitHub Actions logs
3. اختبر البوت محلياً: `python bot.py`

---

**✨ البوت جاهز 100% - فقط اتبع الخطوات أعلاه!** 🚀
