name: 🤖 بوت أخبار المنطقة الشرقية

on:
  # تشغيل تلقائي كل 6 ساعات
  schedule:
    - cron: '0 */6 * * *'  # كل 6 ساعات
  
  # تشغيل يدوي من GitHub
  workflow_dispatch:

jobs:
  send-news:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout الكود
        uses: actions/checkout@v4
      
      - name: 🐍 تثبيت Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 تثبيت المكتبات
        run: |
          pip install -r requirements.txt
      
      - name: 🤖 تشغيل البوت
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        run: |
          python bot.py
      
      - name: 💾 حفظ سجل الأخبار المرسلة
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🔄 تحديث: سجل الأخبار المرسلة"
          file_pattern: 'sent_news.json'
          commit_user_name: 'Eastern Province News Bot'
          commit_user_email: 'bot@github-actions'
          commit_author: 'Bot <bot@github-actions>'
